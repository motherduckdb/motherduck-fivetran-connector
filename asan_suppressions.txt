# ASAN runtime suppressions file
# Use with: ASAN_OPTIONS=suppressions=asan_suppressions.txt
# Supported types: interceptor_name, interceptor_via_fun, interceptor_via_lib, odr_violation

# Suppress errors where the call stack contains DuckDB functions
interceptor_via_fun:duckdb::*

# Suppress errors from DuckDB library
interceptor_via_lib:*duckdb*

# Suppress ODR violations from DuckDB
odr_violation:*duckdb*
